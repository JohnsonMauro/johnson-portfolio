// localStorage keys shared by the React islands and the inline <head> scripts.
// Inline scripts cannot import modules: pass these in with define:vars.
export const THEME_STORAGE_KEY = 'theme';
export const LOCALE_STORAGE_KEY = 'preferred-locale';
